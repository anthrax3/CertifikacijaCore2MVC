@model IEnumerable<CertifikacijaMVC.Models.TipPolaganja>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Tip)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.TipEng)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tip)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipEng)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipPolaganjaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TipPolaganjaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TipPolaganjaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
